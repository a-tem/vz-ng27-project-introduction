import { ToggleOnClickDirective } from './toggle-on-click.directive';

describe('ToggleOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
